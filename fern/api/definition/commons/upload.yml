types: 

  UploadPresignedRequest:
    docs: > 
      A presigned request used to upload files. 
      Upload your file to the given URL via a PUT request.
    properties:
      path:
        type: string
        docs: > 
          The name of the file to upload. 
          This is the same as the one given in the upload prepare file.
      url:
        type: string
        docs: |
          The URL of the presigned request for which to upload your file to.
